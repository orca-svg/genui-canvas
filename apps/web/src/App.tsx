/**
 * Shell entry. The interactive card canvas + chat + orchestrator wiring land in
 * M3; for now this is a placeholder over the M2 shell-state core.
 */
export function App() {
  return (
    <main className="genui-canvas-app">
      <h1>genui-canvas</h1>
      <p>Interaction-driven generative UI over the mcp-gen-ui-gateway. Shell state ready; orchestrator wiring in M3.</p>
    </main>
  );
}
