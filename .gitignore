node_modules/
dist/
*.tsbuildinfo
.DS_Store
.env
.env.local
coverage/

# Local interaction-trace data (never commit study data)
data/sessions/
*.jsonl

# Local-only agent harness (verification criteria live here + docs/verification.md)
.claude/

# Playwright MCP scratch output
.playwright-mcp/

# Mirrored local-only agent harness (same policy as .claude/ — never commit)
.agents/
.Codex/
AGENTS.md

# Local status / handoff notes (personal, not part of the repo)
/PROJECT_STATUS.md
